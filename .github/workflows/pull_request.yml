name: Pull request

on:
  branch_protection_rule:
  pull_request:
    paths-ignore:
      - 'doc/**'
      - 'enhancements/**'
      - '*.md'
    banches:
      - main

jobs:
  golang:
    uses: ./.github/workflows/gofmt.yml
  gosec:
    uses: ./.github/workflows/gosec.yml
  local_dev:
    uses: ./.github/workflows/local_dev.yml
  unit_test:
    uses: ./.github/workflows/unit_test.yml
  unit_test_mac:
    uses: ./.github/workflows/unit_test_mac.yml
  integration_test:
    uses: ./.github/workflows/integration_test.yml
  integration_test_libbpf:
    uses: ./.github/workflows/integration_test_libbpf.yml

  
